# ENG-49 Cash Operations Evidence

Generated by `tests/e2e/cash-operations.spec.ts` on 2026-07-24.

- `cash-light-1440.png`: multiple-professional open day in the light theme at 1440 × 900.
- `cash-dark-320.png`: the same semantic flow in the dark theme at 320 CSS pixels, also exercising
  the 640-pixel layout equivalent at 200% zoom.
- `cash-forced-colors-320.png`: the narrow route under browser forced-colors emulation.

The focused browser run verifies authenticated `Caixa` navigation, an exact close, immutable
read-only state, non-zero-difference reason association and first-invalid focus, atomic failure and
retry, already-closed state, 24-record bounded history, read-only detail, deterministic reload,
URL privacy, keyboard traversal, confirmation focus, reduced motion, 24-pixel targets, absence of
document-level horizontal overflow, and axe WCAG 2.2 A/AA rules.

The DOM uses native form controls, associated labels/descriptions/errors, `aria-invalid`, a polite
difference announcement, source-ordered headings, status text independent of color, definition
lists for exact totals, and focus-managed Base UI confirmation semantics. This is automated
screen-reader-semantics evidence; it does not claim a manual VoiceOver/NVDA session or a physical
coarse-pointer device.
