# Initiative 18 Product QA recheck — MVP-S1/S2 real-stack

- Date: 2026-08-27
- Branch: `feature/mvp-readiness-and-schema-baseline`
- Worktree SHA: `402e79b1bcb05dfa02879f45dee84e820eeabfcb`
- Baseline: `2026-08-27-402e79b1bcb0-r2`
- Environment: local controllable Playwright, real Astro/Vite/Bun/Elysia,
  unique disposable PostgreSQL
- Decision: **approved with reservations**
- Weighted score: **9.5/10**

## Outcome

The corrected integrated suite passed twice consecutively, 3/3 in 24.4 seconds
and 3/3 in 27.1 seconds. The reruns proved the
empty-database migration chain, all required campaign-link states, consent-gated
privacy-safe analytics, the real MVP-S2 intake and persistence path, and
database outage recovery. All final desktop/mobile screenshots were inspected.

This is approved evidence for the tested pre-commit worktree only. The complete
suite and visual inspection must be repeated after the final commit, and the
next immutable report must use that committed PR SHA.

## Real stack and isolation

- Astro site: `127.0.0.1:3103`
- Vite web: `127.0.0.1:3102`
- Bun/Elysia API: `127.0.0.1:8100`
- PostgreSQL: new unique cluster directly beneath OS `tmpdir`, migrated from
  empty state and populated only with synthetic fixtures
- Campaign allowlist: `127.0.0.1` only in both API and site test processes
- Analytics: local Playwright PostHog substitute; no provider network
- External provider network: Cloudflare Siteverify only, with official dummy
  keys; notification stayed on closed loopback
- External dispatch: inherited Trigger.dev secret, URL, project, environment,
  and API-key variables were removed from the API child environment
- PostgreSQL port: allocated after `initdb`, immediately before `pg_ctl`; the
  unavoidable socket-release/bind TOCTOU window remains minimal and fails into
  exact-directory cleanup if contended

No shared/deployed database or real PII was used. No suite-owned PostgreSQL
process remained active after shutdown.

## MVP-S1 campaign links — passed

### Active and history replacement

- Synthetic active/unexpired link resolved through the real API with HTTP 200.
- Destination was exactly
  `http://127.0.0.1:3103/?campaign=e2e-active`, explicitly allowlisted.
- Browser Back returned directly to `/privacy`, skipping the campaign URL. This
  proves observable `location.replace` history behavior.
- Campaign entry initialized no analytics.
- Product console errors, failed HTTP responses, and non-navigation request
  failures were empty.

The Astro Dev Toolbar emitted one known audit error during document replacement
and the redirect aborted pending development assets with `net::ERR_ABORTED`.
Both were classified separately from product errors; neither exists in the
production UI contract.

### Permanent unavailable states

- Direct API checks returned code-only 404 for inactive and expired fixtures.
- Invalid slug was rejected client-side without a private API request.
- Inactive, expired, and invalid produced identical eyebrow, heading, message,
  home action, focused-heading behavior, and mobile layout.
- Screenshots were inspected at 390×844; the focus ring, content, and action fit
  without clipping or horizontal overflow.

### Temporary failure and retry

- With the owned PostgreSQL process stopped, active resolution returned the
  public code-only 503 contract.
- The UI rendered “Falha temporária”, a focused recovery heading, “Tentar
  novamente”, and the safe home action.
- After the same cluster resumed, visible Retry returned HTTP 200 and navigated
  to the allowlisted destination.

## Analytics privacy — passed

- Zero analytics requests before consent.
- Zero analytics requests after “Só essenciais”, including after a tagged CTA.
- Analytics initialized only after “Permitir analytics” in a clean context.
- `cta_clicked` emitted only after opt-in.
- Recorded property keys excluded name, email, phone, company, message, project
  type, lead, and payload fields.
- No analytics body, property value, identifier, lead payload, or PII was
  printed or preserved in reports.

## MVP-S2 intake — passed

- The visible Astro form used accessible labels, explicit contact consent, and
  the official dummy Turnstile contract.
- The real API returned HTTP 201 and the stabilized success state rendered.
- A bounded aggregate returned exactly one processed delivery and one lead.
- After reload the form was usable and the aggregate remained
  `{ deliveries: 1, leads: 1 }`.
- Console errors, failed requests, and HTTP responses >=400 were empty.
- Desktop and mobile form/success/reload screenshots were inspected without
  clipping, overlap, blur, dev toolbar, or PII.

## Migration and resilience — passed

- Migrations `0000` through `0021` completed on a new empty cluster before
  fixture insertion.
- During database outage, `/health` remained 200 and `/ready` became 503.
- After recovery of the same cluster, `/ready` returned to 200.
- Cleanup used the run-specific guarded path and graceful SIGTERM window.

## Inspected screenshots

MVP-S1 artifact directory:

`apps/web/tests/e2e-integrated/.artifacts/test-results/real-stack-MVP-S1-verifies-c05a8-nd-real-campaign-resolution-chromium/`

- `mvp-s1-desktop-home.png`
- `mvp-s1-essential-only.png`
- `mvp-s1-analytics-consented.png`
- `mvp-s1-desktop-privacy.png`
- `mvp-s1-mobile-menu.png`
- `mvp-s1-campaign-active-destination.png`
- `mvp-s1-campaign-inactive.png`
- `mvp-s1-campaign-expired.png`
- `mvp-s1-campaign-invalid.png`
- `mvp-s1-campaign-temporary-failure.png`

MVP-S2 artifact directory:

`apps/web/tests/e2e-integrated/.artifacts/test-results/real-stack-MVP-S2-accepts--ba0dc--one-delivery-with-one-lead-chromium/`

- `mvp-s2-empty-form.png`
- `mvp-s2-success.png`
- `mvp-s2-mobile-success.png`
- `mvp-s2-mobile-after-reload.png`

## Verification

- `bunx biome check apps/web/tests/e2e-integrated` — passed, 6 files.
- `bun --filter web typecheck` — passed.
- `bun --filter web test:e2e:integrated` — two consecutive corrected runs:
  3 passed in 24.4 seconds; 3 passed in 27.1 seconds.
- Companion JSON report — validated against
  `docs/product-qa/report.schema.json`.

## Findings and reservation

- `I18-QA-MIGRATION-1` — fixed and rechecked.
- `I18-QA-CAMPAIGN-1` — fixed and rechecked.
- `I18-QA-CAMPAIGN-2` — fixed and rechecked; composed inactive/expired mapping
  now returns 404.
- `I18-QA-ASTRO-DEV-1` — accepted low environment noise, isolated from product
  console/network evidence.
- No blocker/high finding remains in the executed worktree.

Reservation: this run predates the final commit. Repeat the complete 3/3 suite,
inspect new artifacts, and publish a new immutable report at the final PR SHA
before treating Product QA as final preflight evidence.
