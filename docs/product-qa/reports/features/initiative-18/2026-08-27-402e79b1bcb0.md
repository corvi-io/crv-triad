# Initiative 18 Product QA — MVP-S1/S2 exercisable journeys

- Date: 2026-08-27
- Branch: `feature/mvp-readiness-and-schema-baseline`
- Commit under evaluation: `402e79b1bcb05dfa02879f45dee84e820eeabfcb`
- Environment: local controllable Playwright, real Astro/Vite/Bun/Elysia/PostgreSQL stack
- Decision: **approved with reservations**
- Weighted score: **9.0/10**

## Outcome

The real-stack suite passed 3/3 twice consecutively after corrective evidence
work. The public contact journey returned HTTP 201, rendered the visible success
state, and retained the safe aggregate `{ deliveries: 1, leads: 1 }` after page
reload. No request payload, lead data, credentials, private headers, or other PII
was printed or copied into this report.

The exercised MVP-S1 subset passed desktop public browsing, keyboard skip-link
navigation, legal access, and mobile menu semantics. Campaign-entry resolution
and analytics-after-consent were not exercised by this scaffold, so this report
does not approve those portions of the full MVP-S1 matrix row.

## Real stack and isolation

- Astro site: `127.0.0.1:3103`
- Vite web: `127.0.0.1:3102`
- Bun/Elysia API: `127.0.0.1:8100`
- PostgreSQL: unique cluster under the OS temporary directory for each run,
  guarded cleanup, unique loopback port, migrations applied, synthetic fixture
  only
- External network: Cloudflare Siteverify only, using official dummy keys;
  browser Turnstile script fulfilled locally; notification endpoint disabled on
  closed loopback

No Maestri Portal was connected when `maestri list` was run. Per explicit human
authorization, local controllable Playwright was used. This is an environment
limitation, not a blocker.

## Journeys and evidence

### I18-MVP-S1-PUBLIC — passed

- Desktop home exposes the main landmark, primary navigation, and one visible
  level-one heading.
- Keyboard Tab focuses “Pular para o conteúdo”; Enter moves focus to
  `#main-content`.
- Privacy policy is reachable and readable.
- At 390×844, the menu opens as a named dialog with named mobile navigation.
- Browser console errors, page errors, failed requests, and HTTP responses >=400
  were all empty for the journey.

Inspected screenshots:

- `apps/web/tests/e2e-integrated/.artifacts/test-results/real-stack-MVP-S1-supports-6a5f2-access-and-mobile-semantics-chromium/mvp-s1-desktop-home.png`
- `apps/web/tests/e2e-integrated/.artifacts/test-results/real-stack-MVP-S1-supports-6a5f2-access-and-mobile-semantics-chromium/mvp-s1-desktop-privacy.png`
- `apps/web/tests/e2e-integrated/.artifacts/test-results/real-stack-MVP-S1-supports-6a5f2-access-and-mobile-semantics-chromium/mvp-s1-mobile-menu.png`

### I18-MVP-S2-INTAKE — passed

- The visible Astro form was completed through accessible labels, explicit
  privacy consent, and the dummy Turnstile token.
- The real API response was HTTP 201 and the success state became fully visible.
- A bounded aggregate query returned exactly one delivery and one lead.
- After reload, the form was usable again and the aggregate remained exactly
  one delivery and one lead.
- Desktop and mobile empty/success states were inspected with no clipping,
  toolbar overlay, blurred transition, or obstructed controls in the final run.
- Browser console errors, page errors, failed requests, and HTTP responses >=400
  were all empty for the journey.

Inspected screenshots:

- `apps/web/tests/e2e-integrated/.artifacts/test-results/real-stack-MVP-S2-accepts--ba0dc--one-delivery-with-one-lead-chromium/mvp-s2-empty-form.png`
- `apps/web/tests/e2e-integrated/.artifacts/test-results/real-stack-MVP-S2-accepts--ba0dc--one-delivery-with-one-lead-chromium/mvp-s2-success.png`
- `apps/web/tests/e2e-integrated/.artifacts/test-results/real-stack-MVP-S2-accepts--ba0dc--one-delivery-with-one-lead-chromium/mvp-s2-mobile-success.png`
- `apps/web/tests/e2e-integrated/.artifacts/test-results/real-stack-MVP-S2-accepts--ba0dc--one-delivery-with-one-lead-chromium/mvp-s2-mobile-after-reload.png`

### I18-DB-RESILIENCE — passed

With the owned PostgreSQL process stopped, `/health` remained 200 while
`/ready` became 503. After the same cluster resumed, `/ready` recovered to 200.
Cleanup remained restricted to the unique run directory beneath the OS
temporary directory.

## Corrective rechecks

Three evidence-only defects were corrected inside the test scaffold:

1. The cookie banner initially covered form evidence; the journey now explicitly
   chooses essential-only analytics before intake capture.
2. Success was initially captured during a CSS transition; capture now waits for
   computed `filter: none` and `opacity: 1`.
3. Astro's development toolbar and a short desktop viewport obscured evidence;
   the scaffold hides only the dev-only toolbar in the test document and uses a
   1440×1200 success viewport.

No production source was changed. Final corrective reruns:

- Run 1: 3 passed in 14.9 s.
- Run 2: 3 passed in 16.1 s.

## Verification

- `bunx biome check apps/web/tests/e2e-integrated` — passed, 6 files checked.
- `bun --filter web typecheck` — passed.
- `bun --filter web test:e2e:integrated && bun --filter web test:e2e:integrated`
  — passed 3/3 twice consecutively.
- JSON schema validation command — passed; see companion JSON report.

## Reservations

- No Maestri Portal was connected; authorized local Playwright supplied the
  browser controls and screenshot evidence.
- Campaign-entry happy path and analytics-after-consent remain outside this
  executed scaffold and need their own real fixtures/evidence before the whole
  MVP-S1 matrix row can be approved.
- Accessibility evidence covers semantic landmarks, accessible names, keyboard
  focus, dialog/navigation roles, and responsive inspection; it is not a full
  assistive-technology matrix.
