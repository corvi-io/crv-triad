# Initiative 18 final Product QA — full real-stack

- Date: 2026-08-28
- Branch: `feature/mvp-readiness-and-schema-baseline`
- Frozen runtime SHA: `9a7589618d16660e5a170c265f6e5863b98cb71a`
- Environment: real Astro/Vite/Bun/Elysia, fresh disposable PostgreSQL, Playwright, and connected Maestri Portals
- Decision: **approved**
- Weighted score: **9.8/10**

## Outcome

The corrective gate passed 3/3, then exactly two complete consecutive runs
passed 6/6 with one worker and Playwright retries set to zero. Run 1 completed
in 1:18.21 wall time (reported 1.3m); run 2 completed in 1:36.63 wall time
(reported 1.6m). Each run started an empty, unique PostgreSQL cluster, applied
migrations 0000 through 0021, used only synthetic fixtures, and produced 31
screenshots. No runtime blocker remains on the frozen SHA.

## Covered outcomes

- S1: active campaign `location.replace`, indistinguishable
  inactive/expired/invalid permanent UX, temporary failure/retry, privacy-safe
  analytics only after consent, zero analytics before consent and after
  essential-only, desktop/mobile navigation, keyboard and reduced motion.
- S2: visible public intake returned 201 and persisted the safe aggregate
  `{ deliveries: 1, leads: 1 }` after reload.
- Readiness: empty-database migration chain 0000-0021; liveness stayed 200 while
  readiness became 503 during the owned outage and recovered to 200.
- IDP/workspace: real Better Auth sign-in/private cookies, invitation and signup,
  overview, administration, profile, refresh/logout, anonymous guard, admin,
  member, and disabled-user boundaries.
- C1: create/list/Kanban/search/filter/history/pagination/detail/stage,
  archive/restore, and stale-version 409.
- C2: activity lifecycle, rollback behavior, and audio boundary.
- C3: configured fields plus flat work queue complete/dismiss; aggregate
  `activities=1`, `finishedActions=2`, `pendingActions=0`.
- C4: anonymous API 401; member UI denied with zero connector heading, tabs,
  list, counts, browser queries, or cached data and direct API 403; admin
  create/configure/map/activate/test delivery to **Processado**, signed
  idempotency, and secret rotation. Aggregate evidence was `connectors=1`,
  `activeConnectors=1`, `processedDeliveries=2`, `deliveryBackedLeads=2`, and
  `rawPayloadColumns=0`.

No password, cookie, token, connector secret, request payload, raw delivery, or
PII was printed or preserved. External dispatch/provider environments were
neutralized; only the official Siteverify dummy-key contract was used.

## Visual, accessibility, console, and network evidence

Both final runs captured 31 PNGs: 10 S1, 4 S2, 7 IDP/workspace, 6 C1-C3, and
4 C4. Run 1 is preserved under
`apps/web/tests/e2e-integrated/.artifacts/final-run-1`; run 2 is under
`apps/web/tests/e2e-integrated/.artifacts/test-results`. All 31 run-2 images and
representative run-1 checkpoints were inspected. They cover 1440px desktop,
390×844 mobile, and 200% zoom without blocking clipping, overlap, or loss of
operability.

The connected **Initiative 18 Desktop** (1440×1000) and **Initiative 18
Mobile** (390×844) Portals were also inspected through accessibility snapshots,
logs, and rendered screenshots. Semantic headings, links, labels, and buttons
were present; the live site and mobile login were visually sound; Portal log
buffers were empty. Playwright product console, pageerror, failed-response, and
failed-request collectors were clean.

One accepted low-severity limitation remains: Astro Dev Toolbar emits a
development-only `Failed to fetch` audit message while the campaign flow
replaces the document. It is isolated from product collectors and does not
alter the tested outcome.

## Access and tenant statement

The product is workspace-global and has no tenant model, so tenant isolation is
explicitly **N/A**. The applicable substitute boundary was proved across
anonymous, member, admin, and disabled identities through both UI and API.

## Verification

- `bun --filter web test:e2e:integrated -- workspace-real-stack.spec.ts` —
  3 passed in 59.2s.
- `bun --filter web test:e2e:integrated` — run 1: 6 passed in 1.3m,
  wall 1:18.21, retries 0.
- `bun --filter web test:e2e:integrated` — run 2: 6 passed in 1.6m,
  wall 1:36.63, retries 0.
- `bunx biome check apps/web/tests/e2e-integrated` — passed.
- `bun --filter web typecheck` — passed.
- Companion JSON — validated against `docs/product-qa/report.schema.json`.

The final stack was stopped through owned process handles. Ports 3102, 3103,
and 8100 had no listeners, and the final run-owned PostgreSQL directory was
absent. Historical temporary directories whose ownership could not be proven
were intentionally left untouched.
